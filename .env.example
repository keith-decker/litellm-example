# Environment variables for opentelemetry_poem.py

# OpenAI API Key (required to use OpenAI models via LiteLLM)
# Replace the value with your real key
OPENAI_API_KEY=sk-your-real-openai-key

# OpenTelemetry OTLP gRPC endpoint
# Set this to your collector's OTLP gRPC endpoint. Default collector listens on 4317.
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# Optional: if your collector requires headers (e.g., x-otlp-api-key), uncomment and set
# OTEL_EXPORTER_OTLP_HEADERS=x-otlp-api-key=your-observability-api-key

# Optional: explicitly set protocol (grpc or http/protobuf). We use gRPC exporter in code.
OTEL_EXPORTER_OTLP_PROTOCOL=grpc

# Optional: override the service name from code. Our code sets service.name=litellm-poetry already.
# OTEL_SERVICE_NAME=litellm-poetry

LITELLM_OTEL_INTEGRATION_ENABLE_EVENTS=true
LITELLM_OTEL_INTEGRATION_ENABLE_METRICS=true
